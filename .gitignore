node_modules/
_site/
.DS_Store
*.log
.env
.env.local
public/

# Brainstorming companion sessions
.superpowers/

# Locally-generated PR body drafts
pr-body.md

# Per-user Claude Code permission settings (must not commit — leaks paths)
.claude/settings.local.json

# Superpowers spec/plan artifacts — kept in sibling dir vatt-ghern-specs/
docs/superpowers/
.playwright-mcp/

# Generated at deploy time by `npm run build` (prebuild → build-og + build-card).
# Cloudflare Pages must run `npm ci && npm run build` so these regenerate fresh.
# NOT committed: og PNGs change with every post, today.svg changes daily.
# (Input asset vg-sigil.png lives in src/static/, not src/static/og/, so it stays tracked.)
src/static/og/
src/static/today.svg
