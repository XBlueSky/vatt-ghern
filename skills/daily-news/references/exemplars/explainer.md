# Exemplar: deep-turso-quint-sqlite — explainer

**Why this is the exemplar:** The opener teaches the reader exactly what's at stake before any explanation begins: "Turso 工程師寫了一個只描述「open → create table → serialize → start reading → call sqlite3_deserialize() during active read transaction」的 Quint 模型，五步驟跑出來，SQLite 直接 crash 而不是回 SQLITE_BUSY——這份被測試了二十年的程式碼，倒在了一個五步的形式化 trace 上" — a specific 5-step trace, a real API call, a real error code, and a 20-year-tested system as the crash victim. The 5-beat explainer ladder (concrete case → prereq concept "state-space 是什麼" → idea "Quint 是什麼" → mechanism comparison "simulator 與 model checker" → worked example with C reproducer + 12-bug table → applicability checklist) builds from where the reader stands rather than assuming familiarity with formal methods. The "state-space 是什麼" H2 derives the |A|^N explosion from first principles and frames fuzzer-vs-formal-model as a dimensionality argument the source itself did not articulate.

**Watch-for:** The two "X 是什麼" H2s ("state-space 是什麼" / "Quint 是什麼") share a phrasing pattern that anti-template axis docks for parallel structure — both Opus reviewers gave Anti-template 9 not 10 for this. Do not copy this exemplar's H2 scaffolding ("X 是什麼", numbered ladder), opener structure (specific trace → unexpected outcome → irony anchor), or the C-reproducer + bug-table device. The bar is teaching prereq concepts from first principles before introducing the new technology, and grounding applicability with concrete cost estimates (3-5 engineer days per API, 2-3 weeks team training).

**Source:** src/posts/2026/05/20/deep-turso-quint-sqlite.html. Audit score (Opus consensus): Hook 9 / Structural 9 / Material 9 / Depth 9 / Relevance 9 / Anti-template 9 = 54.
