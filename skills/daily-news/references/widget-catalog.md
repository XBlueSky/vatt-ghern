<!-- AUTO-GENERATED from src/_includes/widgets/*.widget.json — run `npm run widgets:catalog`. Do not hand-edit. -->

# Catalog widgets — summon with `{% widget "name" %}`

These are finished, reusable widgets. When a deep-story's concept question
matches a widget's **suits**, summon it instead of hand-writing an inline
widget. A summoned catalog widget counts toward the ≥5-widget floor and
satisfies the ≥1-interactive rule. Do NOT force a match — a contrived cast
is worse than a clean inline widget.

## Feature flags — `feature-flags`

feature flag 的即時 JSON 檢視：每個環境各有開關、可調 rollout 百分比，任何更動都會即時重新產生輸出。

- **suits**: flag-matrix-demo, rollout-explainer, config-shape-walkthrough
- **interactive**: toggle-per-env, percentage-slider, live-json-output
- **usage**: `{% widget "feature-flags" %}`

