<style>
  .vg-w-demo-tab-switcher-pure-css {
    margin: var(--s-3) 0 var(--s-4);
    border: 1px solid var(--line); border-radius: 6px;
  }
  .vg-w-demo-tab-switcher-pure-css input[type="radio"] {
    position: absolute; opacity: 0; pointer-events: none;
  }
  .vg-w-demo-tab-switcher-pure-css .tablist {
    display: flex;
    border-bottom: 1px solid var(--line);
  }
  .vg-w-demo-tab-switcher-pure-css .tablist label {
    flex: 1;
    min-height: 44px;
    display: inline-flex; align-items: center; justify-content: center;
    padding: var(--s-2) var(--s-3);
    font-family: var(--sans); font-size: var(--fs-sm);
    letter-spacing: 0.04em;
    color: var(--ink-soft);
    cursor: pointer;
    border-right: 1px solid var(--line);
    transition: background 120ms, color 120ms;
  }
  .vg-w-demo-tab-switcher-pure-css .tablist label:last-child { border-right: none; }
  .vg-w-demo-tab-switcher-pure-css .tablist label:hover { background: var(--bg-soft); }

  /* Active tab —— bg-soft 強調 + 底部 2px accent underline */
  .vg-w-demo-tab-switcher-pure-css:has(#tab-a:checked) label[for="tab-a"],
  .vg-w-demo-tab-switcher-pure-css:has(#tab-b:checked) label[for="tab-b"],
  .vg-w-demo-tab-switcher-pure-css:has(#tab-c:checked) label[for="tab-c"] {
    color: var(--accent-text); font-weight: 600;
    background: var(--bg-soft);
    box-shadow: inset 0 -2px 0 var(--accent);
  }

  /* Focus —— radio 隱藏但 Tab 仍會 focus 進去;:has(:focus-visible) 把
     outline 顯到對應的 label 上,鍵盤使用者才知道焦點在哪。原生 radio
     本來就支援 Arrow Left/Right 在 group 內切換 + 自動 check,不必加 JS。 */
  .vg-w-demo-tab-switcher-pure-css:has(#tab-a:focus-visible) label[for="tab-a"],
  .vg-w-demo-tab-switcher-pure-css:has(#tab-b:focus-visible) label[for="tab-b"],
  .vg-w-demo-tab-switcher-pure-css:has(#tab-c:focus-visible) label[for="tab-c"] {
    outline: 2px solid var(--accent-text);
    outline-offset: -2px;
  }

  .vg-w-demo-tab-switcher-pure-css .panel {
    display: none;
    padding: var(--s-3) var(--s-4);
    font-family: var(--serif);
    font-size: var(--fs-sm);
    color: var(--ink-soft);
    line-height: 1.65;
  }
  .vg-w-demo-tab-switcher-pure-css .panel code {
    font-family: var(--mono); font-size: 0.92em;
    background: var(--bg-soft); padding: 0.05em 0.32em; border-radius: 3px;
    color: var(--ink);
  }
  .vg-w-demo-tab-switcher-pure-css:has(#tab-a:checked) .panel-a,
  .vg-w-demo-tab-switcher-pure-css:has(#tab-b:checked) .panel-b,
  .vg-w-demo-tab-switcher-pure-css:has(#tab-c:checked) .panel-c { display: block; }
</style>

<figure class="vg-w-demo-tab-switcher-pure-css">
  <input type="radio" id="tab-a" name="vg-w-demo-tab-switcher-pure-css" checked />
  <input type="radio" id="tab-b" name="vg-w-demo-tab-switcher-pure-css" />
  <input type="radio" id="tab-c" name="vg-w-demo-tab-switcher-pure-css" />
  <div class="tablist" role="tablist">
    <label for="tab-a" role="tab">overview</label>
    <label for="tab-b" role="tab">benchmark</label>
    <label for="tab-c" role="tab">notes</label>
  </div>
  <div class="panel panel-a" role="tabpanel">兩個系統的差異主要在 <code>ld</code> binding 順序 —— 當 <code>dlopen</code> 第二個 <code>.so</code> 時,cgi 模式會踩到 binding 雷,worker 不再可被 reuse。</div>
  <div class="panel panel-b" role="tabpanel">p50 差異:morpheus <code>2.1ms</code> vs synoscgi <code>5.6ms</code>,**2.7×**。p99 在 reuse 開的條件下差距縮小到 **1.8×**。</div>
  <div class="panel panel-c" role="tabpanel">2021/01 內部 mail《WebAPI Worker Reuse Investigate》是改 <code>env-guard</code> 路徑的源頭;`SZK_ENV_IN_SYNOAPID` 之後才補上。</div>
</figure>
