<style>
  .vg-w-demo-css-3d-transform {
    display: grid; gap: var(--s-3);
    margin: var(--s-3) 0 var(--s-4);
  }
  .vg-w-demo-css-3d-transform .stage {
    display: flex; justify-content: center;
    padding: var(--s-5) 0;
    background: var(--bg-soft);
    border: 1px solid var(--line);
    border-radius: 6px;
    /* 第一條必要 CSS:stage 上的 perspective 給整個 3D 場景一個觀察點。
       距離越短透視越強;800px ≈ 自然的桌上盒體感。 */
    perspective: 800px;
  }
  .vg-w-demo-css-3d-transform .cube {
    position: relative;
    width: 120px; height: 120px;
    /* 第二條必要 CSS:preserve-3d 讓子元素各自在 3D 空間擺位,而不是被
       cube 自己的平面投影壓平。少這條 → 6 面全變平面同一張卡。 */
    transform-style: preserve-3d;
    animation: c3d-spin 14s linear infinite;
  }
  .vg-w-demo-css-3d-transform .stage:hover .cube { animation-play-state: paused; }

  .vg-w-demo-css-3d-transform .face {
    position: absolute; inset: 0;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    gap: 0.3em;
    /* 第三條必要 CSS:每面自己的 transform 推到 3D 空間中對應位置。
       translateZ(60) 把面往觀察者方向推半個邊長,旋轉就把它擺正確面向。 */
    background: color-mix(in oklch, var(--accent), transparent 78%);
    border: 1px solid var(--accent);
    backface-visibility: visible;
  }
  .vg-w-demo-css-3d-transform .face .letter {
    font-family: var(--display); font-style: italic;
    font-size: 28px; color: var(--accent-text);
    font-weight: 500; line-height: 1;
  }
  .vg-w-demo-css-3d-transform .face .formula {
    font-family: var(--mono); font-size: 9.5px;
    color: var(--ink-soft);
    text-align: center; line-height: 1.2;
    padding: 0 0.4em;
    max-width: 100%;
  }

  /* 6 face transforms —— half-edge = 60px(cube 120/2) */
  .vg-w-demo-css-3d-transform .face.f1 { transform: translateZ(60px); }
  .vg-w-demo-css-3d-transform .face.f2 { transform: translateZ(-60px) rotateY(180deg); }
  .vg-w-demo-css-3d-transform .face.f3 { transform: rotateY(90deg)  translateZ(60px); }
  .vg-w-demo-css-3d-transform .face.f4 { transform: rotateY(-90deg) translateZ(60px); }
  .vg-w-demo-css-3d-transform .face.f5 { transform: rotateX(90deg)  translateZ(60px); }
  .vg-w-demo-css-3d-transform .face.f6 { transform: rotateX(-90deg) translateZ(60px); }

  @keyframes c3d-spin {
    from { transform: rotateX(-20deg) rotateY(0); }
    to   { transform: rotateX(-20deg) rotateY(360deg); }
  }
  @media (prefers-reduced-motion: reduce) {
    .vg-w-demo-css-3d-transform .cube { animation: none; transform: rotateX(-20deg) rotateY(35deg); }
  }

  .vg-w-demo-css-3d-transform figcaption {
    font-family: var(--serif); font-style: italic;
    font-size: var(--fs-sm); color: var(--ink-soft);
    line-height: 1.6; margin: 0;
  }
  .vg-w-demo-css-3d-transform figcaption code {
    font-family: var(--mono); font-style: normal; font-size: 0.92em;
    background: var(--bg-soft); padding: 0.05em 0.32em; border-radius: 3px;
  }
  .vg-w-demo-css-3d-transform figcaption ol {
    margin: var(--s-2) 0; padding: 0;
    list-style: none; counter-reset: rule;
    display: grid; gap: 0.35em;
  }
  .vg-w-demo-css-3d-transform figcaption ol li {
    position: relative;
    padding-left: 2em;
    font-style: normal; color: var(--ink);
  }
  .vg-w-demo-css-3d-transform figcaption ol li::before {
    counter-increment: rule;
    content: counter(rule) '.';
    position: absolute; left: 0; top: 0;
    font-family: var(--mono); font-variant-numeric: tabular-nums;
    font-size: var(--fs-xs); font-weight: 600;
    color: var(--accent-text); letter-spacing: 0.04em;
  }
  .vg-w-demo-css-3d-transform figcaption ol li b {
    font-family: var(--mono); font-style: normal;
    font-weight: 600; color: var(--accent-text);
  }
  .vg-w-demo-css-3d-transform figcaption .footnote {
    margin-top: var(--s-2);
    color: var(--muted);
  }
</style>

<figure class="vg-w-demo-css-3d-transform">
  <div class="stage">
    <div class="cube">
      <span class="face f1"><span class="letter">F</span><code class="formula">translateZ(60)</code></span>
      <span class="face f2"><span class="letter">B</span><code class="formula">translateZ(−60) rotateY(180)</code></span>
      <span class="face f3"><span class="letter">R</span><code class="formula">rotateY(90) translateZ(60)</code></span>
      <span class="face f4"><span class="letter">L</span><code class="formula">rotateY(−90) translateZ(60)</code></span>
      <span class="face f5"><span class="letter">U</span><code class="formula">rotateX(90) translateZ(60)</code></span>
      <span class="face f6"><span class="letter">D</span><code class="formula">rotateX(−90) translateZ(60)</code></span>
    </div>
  </div>
  <figcaption>
    要讓 6 面合成一個立方體有三條必要 CSS:
    <ol>
      <li><b>perspective: 800px</b> 放 stage 上 —— 給整個 3D 場景一個觀察點。</li>
      <li><b>transform-style: preserve-3d</b> 放 cube 上 —— 別把子元素壓回平面。</li>
      <li><b>transform: rotate/translate(…)</b> 放每面自己 —— 推到對應的 3D 位置。</li>
    </ol>
    <span class="footnote">半邊長 <code>60px</code> = cube 邊長一半。滑鼠移上去 cube 暫停轉,方便對照公式跟結果。</span>
  </figcaption>
</figure>
