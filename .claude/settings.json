{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "test -d node_modules || npm ci --no-audit --no-fund --prefer-offline 2>/dev/null || npm install --no-audit --no-fund --prefer-offline"
          }
        ]
      }
    ]
  }
}
